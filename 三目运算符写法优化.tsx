// 写法一: 三目
let demo1 = (
  <>
    {
      type === 'a' ? (
        <A/>
      ) : (
          type === 'b' ? (
            <B/>
          ) : (
            <C/>
          )
    }
  </>
)

// 写法二：逻辑与
let demo2 = (
  <>
    {
      (type === 'a') && (
        <A/>
      )
    }
    {
      (type === 'b') && (
        <B/>
      )
    }
    {
      (type === 'C') && (
        <C/>
      )
    }
  </>
)

